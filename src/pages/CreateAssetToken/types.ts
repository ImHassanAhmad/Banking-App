export const currencies = [{ label: 'USD' }, { label: 'EUR' }, { label: 'GBP' }];

export const issuerWallets = ['Metamask', 'Enjin Wallet', 'Mist Wallet', 'Trust Wallet'];

export const steps = ['Token Basic Information', 'Token Configuration', 'Token Price'];

export interface InspiredQuestionsListProps {
  defaultSelectedKeys: string[]; // The key of the option that should be selected by default
}
